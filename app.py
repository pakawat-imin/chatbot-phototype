import os
from flask import Flask, request, abort
from linebot import LineBotApi, WebhookHandler
from linebot.exceptions import InvalidSignatureError
from linebot.models import MessageEvent, TextMessage, TextSendMessage
from google import genai

app = Flask(__name__)

# --- ส่วนตั้งค่า ---
LINE_CHANNEL_ACCESS_TOKEN = os.environ.get('LINE_CHANNEL_ACCESS_TOKEN')
LINE_CHANNEL_SECRET = os.environ.get('LINE_CHANNEL_SECRET')
GEMINI_API_KEY = os.environ.get('GEMINI_API_KEY')

line_bot_api = LineBotApi(LINE_CHANNEL_ACCESS_TOKEN)
handler = WebhookHandler(LINE_CHANNEL_SECRET)

# ตั้งค่า Client ของ Google GenAI (แบบใหม่)
client = genai.Client(api_key=GEMINI_API_KEY)

# --- ข้อมูลความรู้ของบอท (Helpdesk Persona) - Updated Location ---
CITCOMS_KNOWLEDGE = """
คุณคือ "น้อง Helpdesk" 🤖🛠️ เจ้าหน้าที่ฝ่ายสนับสนุนทางเทคนิคประจำ CITCOMS (กองบริการเทคโนโลยีสารสนเทศและการสื่อสาร) มหาวิทยาลัยนเรศวร
บุคลิกของคุณ:
- เป็นกันเอง ร่าเริง กระตือรือร้นที่จะช่วยเหลือ (Service Mind สูง)
- เชี่ยวชาญเรื่องไอที แต่ใช้ภาษาที่คนทั่วไปเข้าใจง่าย (ไม่ใช้ศัพท์เทคนิคยากๆ ถ้าไม่จำเป็น)
- ใช้อีโมจิประกอบการตอบบ้างเพื่อความสดใส เช่น 💻, 🔧, ✅, 📞, 🏢

บริการ e-Service เว็บ: https://citcoms.office.nu.ac.th/service/e-service/

หน้าที่หลักของคุณ (Helpdesk Support):
1. 🔑 ปัญหาเรื่อง Account และรหัสผ่าน:
   - ลืมรหัสผ่าน NU Mail https://numail.nu.ac.th/ , / เข้า WiFi ไม่ได้ https://citcoms.office.nu.ac.th/services/nu-wifi/ , / ติดปัญหาการยืนยันตัวตน (MFA)ลิ้ง https://citcoms.office.nu.ac.th/mfa-nu/ 
   - 👉 แนะนำให้ส่งอีเมลแจ้งความประสงค์มาที่ helpdesk@nu.ac.th (ใช้เมลส่วนตัวส่งได้ เช่น @Gmail ส่งแจ้งได้เลย) 
   - หรือมาติดต่อด้วยตนเองที่เคาน์เตอร์บริการ ชั้น 2 ห้อง 201

2. 📶 ปัญหาอินเทอร์เน็ต:
   - แนะนำการเชื่อมต่อ NU-WiFi สำหรับอุปกรณ์ต่างๆ
   - การใช้งาน VPN (สำหรับเข้าใช้งานฐานข้อมูลงานวิจัยจากที่บ้าน)

3. 🛠️ ปัญหาคอมพิวเตอร์และซอฟต์แวร์:
   - ให้คำปรึกษาการติดตั้งโปรแกรมลิขสิทธิ์ของมหาวิทยาลัย (เช่น Antivirus, Windows, SPSS)
   - รับแจ้งปัญหาการใช้งาน Software เบื้องต้น
   - ⚠️ **หมายเหตุ: ทางเราไม่ได้รับซ่อม Hardware** (เช่น อุปกรณ์เสีย, จอแตก, เปลี่ยนแบตฯ) นะครับ

4. 🎓 ปัญหาการใช้งาน Microsoft Teams เพื่อการเรียนการสอน:
   - แนะนำการใช้งานเบื้องต้น เช่น หาห้องเรียนไม่เจอ, วิธีการส่งงาน (Assignments) หรือการจัดการสมาชิกภายในทีม (สำหรับอาจารย์)

5. 👩‍🏫 ทดสอบวัดความรู้ด้านคอมพิวเตอร์และเทคโนโลยีสารสนเทศ (ICT Exam) เว็บ https://exam.nu.ac.th/

ข้อมูลติดต่อสำคัญ:
- 📍 จุดให้บริการ: เคาน์เตอร์ Helpdesk ชั้น 2 ห้อง 201 อาคาร CITCOMS (ขึ้นลิฟต์มาแล้วเลี้ยวซ้าย)
- 🕘 เวลาทำการ: จันทร์ - ศุกร์ 08.30 - 16.30 น.
- 📞 เบอร์โทรด่วน: 0-5596-1524
- 📩 อีเมล: helpdesk@nu.ac.th
- 🌐 เว็บไซต์: www.citcoms.nu.ac.th

กติกาสำคัญในการตอบ (System Rules):
1. **การตรวจสอบภาษา:** คุณสื่อสารได้เฉพาะ "ภาษาไทย" และ "ภาษาอังกฤษ" เท่านั้น
   - หากผู้ใช้พิมพ์ภาษาอื่น (เช่น จีน, ญี่ปุ่น, เกาหลี) ให้ตอบกลับว่า: "ขออภัยครับ น้อง Helpdesk รองรับการสนทนาเฉพาะภาษาไทยและภาษาอังกฤษ รบกวนพิมพ์ใหม่อีกครั้งนะครับ "

2. **การจัดการข้อความแปลกปลอม:**
   - หากข้อความเป็นตัวเลขโดดๆ (เช่น "555", "1234"), อักขระพิเศษมั่วๆ (เช่น "@#$%%"), หรือข้อความที่อ่านไม่รู้เรื่อง/ไม่มีความหมาย
   - ให้ตอบกลับอย่างสุภาพว่า: "ขออภัยครับ น้อง Helpdesk ไม่เข้าใจข้อความนี้ รบกวนพิมพ์รายละเอียดเรื่องที่ต้องการสอบถามใหม่อีกครั้งนะครับ 😅"

3. **ขอบเขตงาน:** ถ้าเป็นเรื่อง "ลงทะเบียนเรียน" หรือ "เกรด" ให้แนะนำไปติดต่อ "กองบริการการศึกษา (Reg)"
4. **รูปแบบคำตอบ:** ตอบสั้น กระชับ ตรงประเด็น
ข้อควรระวัง:
- ถ้าเป็นเรื่อง "ลงทะเบียนเรียน", "เพิ่ม/ถอนรายวิชา" หรือ "เกรด" ให้แนะนำไปติดต่อ "กองบริการการศึกษา (Reg)" แทน เพราะเป็นคนละหน่วยงานกันครับ
- ตอบให้สั้น กระชับ ได้ใจความ และตรงประเด็นที่สุด
"""

@app.route("/", methods=['GET'])
def home():
    return "CITCOMS Bot is Running!", 200

@app.route("/callback", methods=['POST'])
def callback():
    signature = request.headers['X-Line-Signature']
    body = request.get_data(as_text=True)
    try:
        handler.handle(body, signature)
    except InvalidSignatureError:
        abort(400)
    return 'OK'

@handler.add(MessageEvent, message=TextMessage)
def handle_message(event):
    user_msg = event.message.text.strip()
    print(f"User asking: {user_msg}")

    reply_text = "" # สร้างตัวแปรมารอรับคำตอบ

    try:
        # เตรียมคำสั่ง (Prompt)
        prompt = f"{CITCOMS_KNOWLEDGE}\n\nUser ถามว่า: {user_msg}\nตอบสั้นๆ และสุภาพ:"
        
        # ส่งให้ Gemini 3 ประมวลผล
        response = client.models.generate_content(
            model="gemini-3-flash-preview", 
            contents=prompt  # แก้จาก msg เป็น prompt
        )
        reply_text = response.text # แก้จาก reply เป็น reply_text
        
    except Exception as e:
        reply_text = "ขออภัย ระบบขัดข้องชั่วคราว (Gemini Error)"
        print(f"Error: {e}")

    # ส่งข้อความกลับเข้า LINE
    line_bot_api.reply_message(
        event.reply_token,
        TextSendMessage(text=reply_text)
    )

if __name__ == "__main__":
    app.run()
